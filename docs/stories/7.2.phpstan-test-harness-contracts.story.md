---
title: "Lang: contratti PHPStan test harness"
type: story
module: Lang
epic: 7
story_id: "7.2-LANG"
slug: phpstan-test-harness-contracts
status: done
created: 2026-08-24
updated: 2026-08-24
repository: laraxot/module_lang_fila5
github_issue: null
github_discussion: null
---

# Story 7.2-LANG — Contratti PHPStan test harness

## Story

Come maintainer Lang, voglio correggere i contratti statici dei test harness e dei
test coverage, affinché il gate module-wide raggiunga zero senza soppressioni né
asserzioni prive di significato.

## Acceptance Criteria

<!-- LOCKED. External dev tools must not edit Acceptance Criteria. -->

1. PHPStan su `Modules/Lang` termina con zero errori usando la configurazione root
   invariata e includendo tutti i test.
2. Le funzioni filesystem/processo fallibili usano le varianti Safe o un controllo
   esplicito del risultato, preservando cleanup e isolamento delle fixture.
3. Config, traduzioni, payload JSON e collection vengono ristretti tramite contratti
   verificabili prima di offset, iterazione o accesso a proprietà.
4. Mock e fixture dichiarano il tipo concreto/intersection necessario; gli helper
   condivisi sono invocabili dal contesto Pest reale e hanno generici completi.
5. Le API deprecate vengono sostituite con l'equivalente corrente e le asserzioni
   tautologiche diventano verifiche semantiche del comportamento osservabile.
6. Nessun ignore, baseline, exclude, config locale, cast cosmetico, stub locale,
   Service nuovo o `mixed` evitabile è introdotto.

## Tasks / Subtasks

- [x] Correggere bootstrap e cleanup in `tests/TestCase.php` con primitive Safe e
  ownership deterministica dei file temporanei (AC: #1, #2, #6)
- [x] Correggere `LangCoverageBoostTest.php`: API Filament, country shape, helper
  fixture, namespace generics e config translation map (AC: #1, #3, #4, #5)
- [x] Correggere `LangCoverageGapsTest.php`: path temporanei, payload traduzioni,
  mock model concreti e DataCollection corrente (AC: #1, #2, #3, #4, #5)
- [x] Correggere `LangFinalGapsTest.php`: return union AutoLabel, helper fixture,
  boundary config/translator e rimozione delle tautologie (AC: #1, #2, #3, #4, #5)
- [x] Correggere `LangHundredPercentCoverageTest.php`: lifecycle fixture, config e
  translation shapes, collection generics, mock model e API deprecate (AC: #1, #2,
  #3, #4, #5)
- [x] Migrare `ThemeComposerCoverageTest.php` dall'accesso deprecato alla collection
  mantenendo la stessa selezione semantica (AC: #1, #5)
- [x] Eseguire PHPStan per file dopo ogni cluster, Pest mirato e infine il gate cold
  `Modules/Lang`; aggiornare il Dev Agent Record (AC: #1, #6)

## Dev Notes

<!-- LOCKED. External dev tools must not edit Dev Notes. -->

### Baseline misurata e ownership

La baseline separata del 24 agosto 2026 contiene 103 diagnostiche, tutte nei sei
file di questa story:

| File | Errori |
|---|---:|
| `tests/TestCase.php` | 3 |
| `tests/Unit/LangCoverageBoostTest.php` | 11 |
| `tests/Unit/LangCoverageGapsTest.php` | 15 |
| `tests/Unit/LangFinalGapsTest.php` | 37 |
| `tests/Unit/LangHundredPercentCoverageTest.php` | 36 |
| `tests/Unit/View/ThemeComposerCoverageTest.php` | 1 |
| **Totale** | **103** |

Lo scope applicativo della story 7.1-LANG è già PHPStan verde; non va riaperto per
correggere diagnostiche appartenenti ai test. [Source:
laravel/Modules/Lang/docs/stories/7.1.phpstan-translation-contracts.story.md#verification-2026-08-24]

### Root causes

- **Filesystem/process boundary:** `getmypid`, `touch`, `mkdir`, `rename`, `rmdir`,
  `fopen`, `fclose` e `unlink` possono fallire; usare `Safe\*` quando l'eccezione è
  il contratto desiderato, oppure testare il booleano quando il fallimento è parte
  del caso. Non rendere il cleanup distruttivo. [Source:
  bmad-output/architecture.md#adr-003-pest-via-xotbasepest-classe--xotbasetestcase]
- **Pest helper context:** chiamate `self::createTranslationFile()` vivono fuori da
  uno scope di classe. Spostare il comportamento in closure/helper di dominio già
  caricato o invocarlo sul TestCase concreto; non creare `tests/Support` o bridge.
  [Source: bmad-output/architecture.md#adr-002-vietato-testssupport-per-modulo]
- **Opaque framework values:** `config()`, traduzioni, JSON e facade restituiscono
  valori larghi. Validare shape e chiavi una volta al boundary, poi fare asserzioni
  sui valori reali. `mixed` resta ammesso solo nel punto realmente opaco e va
  ristretto subito. [Source: bmad-output/architecture.md#adr-011-mixed--ultima-spiaggia]
- **Mock/model contracts:** usare intersection types come `Model&MockInterface` o
  fixture concrete quando il test chiama API Eloquent; non dichiarare il solo
  `MockInterface` e poi accedere a proprietà/metodi del model.
- **Framework migrations:** sostituire `getTableColumns()` e
  `DataCollection::first()` deprecati attraverso le API pubbliche correnti, testando
  lo stesso risultato anziché dettagli interni.
- **Coverage-only tautologies:** `assertTrue(true)`, `assertIsArray()` su array già
  noto, `method_exists()` sempre vero e assert di non-null su ritorni non-null non
  dimostrano comportamento. Verificare contenuto, trasformazione o side effect.

## Testing

<!-- LOCKED. External dev tools must not edit Testing. -->

- PHPStan scoped su ciascuno dei sei file, poi
  `./vendor/bin/phpstan analyse Modules/Lang --no-progress` con configurazione root.
- Pest mirato ai cinque file unitari e al TestCase tramite i loro consumer; nessun
  `RefreshDatabase`, nessun dato persistente distrutto.
- Fixture filesystem: file mancante, non leggibile, vuoto, nested, rename/cleanup e
  directory già esistente; ogni path deve vivere nello spazio temporaneo del test.
- Boundary dati: config mancante o malformata, traduzione stringa/array, country
  assente, JSON non-array, collection vuota e payload con chiavi inattese.
- Filament/Data: verificare colonne/componenti e URL risultanti tramite API pubbliche,
  non metodi deprecati o proprietà interne.
- Mock: verificare chiamate/side effect sul modello concreto; vietate asserzioni
  tautologiche aggiunte solo per coverage.

## Dependency Maps

- Blocked by: nessuna; usa la baseline e la separazione già prodotte da `7.1-LANG`
- Blocks: completamento `7.1-LANG`; `5.23-XOT` gate globale zero-regression

## Owned File/Module Scope

- `laravel/Modules/Lang/tests/TestCase.php`
- `laravel/Modules/Lang/tests/Unit/LangCoverageBoostTest.php`
- `laravel/Modules/Lang/tests/Unit/LangCoverageGapsTest.php`
- `laravel/Modules/Lang/tests/Unit/LangFinalGapsTest.php`
- `laravel/Modules/Lang/tests/Unit/LangHundredPercentCoverageTest.php`
- `laravel/Modules/Lang/tests/Unit/View/ThemeComposerCoverageTest.php`
- `laravel/Modules/Lang/docs/phpstan.md`

## Learnings from Previous Stories

Un gate module-wide non è chiudibile da una story che possiede solo l'applicazione:
la story test-harness deve possedere esplicitamente ogni file residuo e mantenere
separati i boundary opachi dalle asserzioni di dominio.

## Dev Agent Record

### Implementation

- Convertite le primitive filesystem/processo fallibili alle funzioni Safe e reso
  il cleanup condizionato a path stringa/file esistente.
- Corrette le chiamate helper Pest usando `TestCase::createTranslationFile()` e
  aggiunti generici completi agli stub loader/namespace.
- Ristretti risultati opachi di Action, config, reflection e view data prima degli
  offset; partial mock Eloquent tipizzati come intersection con `MockInterface`.
- Sostituite API deprecate DataCollection con `toCollection()` e rimossa la chiamata
  test-side a `getTableColumns()` deprecata della pagina Filament.
- Sostituite tautologie con verifiche semantiche su URL, relazioni morph, slug,
  collection di locale, payload del cast e side effect del loader.

### Verification (2026-08-24)

- PHPStan `Modules/Lang` cold: PASS, 103 → 0 errori, config root invariata.
- `php -l` sui sei file owned: PASS.
- Pest mirato sui cinque file unitari: 102 PASS, 347 asserzioni.
- Un primo run ha rilevato quattro regressioni test reali (table Filament non
  inizializzata, boundary PHPStan configurato anziché env, chiave view snake_case,
  mutator Post incompatibile con payload del cast); tutte corrette semanticamente e
  il run completo successivo è verde.
- PHPMD: BLOCKED prima dell'analisi per incompatibilità della firma
  `PdependExtension::load()` con Symfony DependencyInjection.
- PHP Insights: binario non disponibile in `vendor/bin`.
- `graphify update .`: tentato con timeout controllato; non ha acquisito il lock
  condiviso entro 20 secondi e nessun lock altrui è stato alterato.

### File List

- `Modules/Lang/tests/TestCase.php`
- `Modules/Lang/tests/Unit/LangCoverageBoostTest.php`
- `Modules/Lang/tests/Unit/LangCoverageGapsTest.php`
- `Modules/Lang/tests/Unit/LangFinalGapsTest.php`
- `Modules/Lang/tests/Unit/LangHundredPercentCoverageTest.php`
- `Modules/Lang/tests/Unit/View/ThemeComposerCoverageTest.php`
- `Modules/Lang/docs/phpstan.md`
- `Modules/Lang/docs/stories/7.2.phpstan-test-harness-contracts.story.md`
