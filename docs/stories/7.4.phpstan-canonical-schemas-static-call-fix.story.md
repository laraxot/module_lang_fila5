---
title: "Lang: fix PHPStan method.staticCall dopo migrazione canonical Schemas"
type: story
module: Lang
epic: 7
story_id: "7.4-LANG"
slug: phpstan-canonical-schemas-static-call-fix
status: done
priority: High
created: 2026-09-07
updated: 2026-09-07
repository: laraxot/module_lang_fila5
github_issue: null
github_discussion: null
related:
  - "./phpstan-Lang-fix.md"
  - "./7.2.phpstan-test-harness-contracts.story.md"
  - "../../../../../bashscripts/ai/wiki/memories/xotbaseresource-inline-delegate-static-call-bug.md"
  - "../../../../../bashscripts/ai/wiki/memories/geo-addressform-schema-reuse-static-call-bug.md"
---

# Story 7.4-LANG — Fix PHPStan method.staticCall dopo migrazione canonical Schemas

## Story

Come maintainer Lang, voglio che i test del modulo consumino `Schemas/{Model}Form::getFormSchema()`
e `Schemas/{Model}Infolist::getInfolistSchema()` come i metodi di istanza che sono realmente,
affinché il modulo torni a 0 errori PHPStan dopo che un'altra sessione (Epic 4, swarm
"filament-canonical-structure") ha rimosso lo schema inline dalla Resource e lo ha spostato
sulle classi `Schemas/` dedicate.

## Contesto

`docs/stories/phpstan-Lang-fix.md` aveva chiuso il modulo a 0 errori il 2026-09-06. Il
2026-09-07, mentre questa sessione faceva la ricognizione richiesta da
`bashscripts/docs/prompts/03-quality-gates.md`, il modulo aveva già ~27 file dirty di
un'altra sessione attiva in parallelo che stava rifattorizzando
`app/Filament/Resources/TranslationFileResource.php` e le sue classi `Schemas/` per allinearle
al pattern canonico (Resource senza `getFormSchema()`/`table()` inline, schema delegato a
`Schemas/{Model}Form`/`Schemas/{Model}Infolist`, entrambe con metodo di **istanza**, coerente
con l'abstract contract di `XotBaseResourceForm`/`XotBaseResourceInfolist`). Quel refactor è
corretto architetturalmente ma ha lasciato indietro `tests/Unit/LangCoverageBoostTest.php`
(chiuso da `7.2-LANG` il 2026-08-24, contro la shape precedente), che chiamava i tre metodi
come se fossero statici.

## Acceptance Criteria

<!-- LOCKED. External dev tools must not edit Acceptance Criteria. -->

1. `./vendor/bin/phpstan analyse Modules/Lang --no-progress --memory-limit=-1` (cache pulita)
   termina con 0 errori, `phpstan.neon` invariato.
2. Nessun `@phpstan-ignore`, nessuna baseline, nessun cast, nessun tipo allargato.
3. Il fix tocca solo il file di test coinvolto nell'errore reale; nessun file della migrazione
   canonical-structure in corso di un'altra sessione viene toccato o committato da questa story.
4. I test del file corretto restano verdi (nessuna assert rimossa o svuotata di significato).
5. Coverage non scende: il test coinvolto continua a esercitare lo stesso comportamento reale
   (schema form/infolist/pages), solo con la sintassi di chiamata corretta.

## Tasks / Subtasks

- [x] Ricognizione: `git status`/`git fetch` su Modules/Lang, verifica claim attivi in
  `docs/chat/INDEX.md` (nessun claim "in-progress" specifico su Lang oltre allo swarm
  Epic 4 generico, che non lista Lang tra i 14 moduli espliciti) (AC: #3)
  Vedi anche `docs/chat/2026-09-06-sonnet5-pest-fixes-plus-unclaimed-modules-wave.md`
  (Lang già chiuso da quella sessione il 2026-09-06) e
  `docs/chat/2026-09-07-filament-canonical-structure-billing-4.1.md` (swarm attivo su
  altri 14 moduli, Lang non incluso nella lista esplicita ma le sue Resource seguono lo
  stesso pattern).
- [x] `bash bashscripts/lock/check.sh` sui 4 file coinvolti (Resource, Form, Infolist, test):
  tutti FREE (AC: #3)
- [x] PHPStan cold su `Modules/Lang`: 3 errori reali, tutti `method.staticCall` nello stesso
  file di test (AC: #1)
- [x] Analisi causa radice: lette le classi `XotBaseResourceForm`/`XotBaseResourceInfolist`
  (Xot) per confermare che `getFormSchema()`/`getInfolistSchema()` sono dichiarati `abstract
  public function` (istanza), mai `static` (AC: #2)
- [x] Fix: 3 call site convertiti da chiamata statica a istanziazione + chiamata di istanza,
  stesso pattern già usato due righe sotto nello stesso test per `TranslationFilesTable`
  (AC: #1, #2, #4)
- [x] Verifica: PHPStan `Modules/Lang` 0 errori; Pest mirato al file 14/14 verdi; PHPMD scoped
  nessun nuovo finding sulle righe toccate; pint scoped pass (AC: #1, #4, #5)
- [x] Aggiornato `docs/coverage.md` con la sezione datata 2026-09-07 (AC: #5)

## Dev Notes

<!-- LOCKED. External dev tools must not edit Dev Notes. -->

### Root cause

Non è un bug applicativo. `Modules/Xot/app/Filament/Resources/Schemas/XotBaseResourceForm.php`
e `XotBaseResourceInfolist.php` dichiarano `getFormSchema()`/`getInfolistSchema()` come metodi
di istanza astratti, risolti via `app(static::class)` dentro `configure()` (a sua volta chiamato
da `XotBaseResource::form()`/`::infolist()`, entrambi `final`). Il test era stato scritto/allineato
a una shape precedente e non aggiornato quando l'altra sessione ha completato la migrazione
canonical-structure sulla Resource. Terza occorrenza dello stesso pattern già mappato in
`bashscripts/ai/wiki/memories/xotbaseresource-inline-delegate-static-call-bug.md` e
`.../geo-addressform-schema-reuse-static-call-bug.md` — qui il consumer non è un override
inline né un componente Filament esterno, ma un test che consuma le proprie classi Schemas.

### Alternative scartate

- Ripristinare `getFormSchema()`/`getInfolistSchema()` statici sulla Resource: violerebbe il
  contratto astratto di `XotBaseResourceForm`/`XotBaseResourceInfolist` (istanza) e romperebbe
  ogni altro consumer del pattern canonico nel resto del repo.
- `app(TranslationFileForm::class)->getFormSchema()` (pattern usato nella memoria Geo): valido
  ma qui la classe non ha dipendenze da container e il file usa già `new TranslationFilesTable()`
  due righe sotto — `new` è più semplice e coerente con il contesto immediato.

## Testing

<!-- LOCKED. External dev tools must not edit Testing. -->

- `./vendor/bin/phpstan analyse Modules/Lang --no-progress --memory-limit=-1` (cache pulita):
  3 → 0 errori.
- `./vendor/bin/pest Modules/Lang/tests/Unit/LangCoverageBoostTest.php -c
  Modules/Lang/phpunit.xml --no-coverage`: 14 passed (58 assertions).
- `./tools/phpmd.sh Modules/Lang text Modules/Lang/phpmd.ruleset.xml`: nessun nuovo finding
  sulle righe 143/154/155.
- `./vendor/bin/pint Modules/Lang/tests/Unit/LangCoverageBoostTest.php --test`: passed.
- Full-suite Pest confermato bloccato da `dddx()` pre-esistente in `SaveTransAction.php`
  (documentato in `docs/coverage.md`, non introdotto da questa story).

## Dependency Maps

- Blocked by: nessuna.
- Blocks: nessuna; ma va coordinata con la sessione che possiede la migrazione
  canonical-structure di `TranslationFileResource` (vedi `docs/chat/INDEX.md`, swarm Epic 4) —
  questa story non tocca né commenta quel refactor, solo il test che ne dipende.

## Owned File/Module Scope

- `laravel/Modules/Lang/tests/Unit/LangCoverageBoostTest.php`
- `laravel/Modules/Lang/docs/coverage.md`
- `laravel/Modules/Lang/docs/stories/7.4.phpstan-canonical-schemas-static-call-fix.story.md`

## Learnings from Previous Stories

Chiudere un modulo a 0 errori (`phpstan-Lang-fix.md`, `7.2-LANG`) non è definitivo in un
repo condiviso con altre sessioni AI attive: un refactor architetturale corretto su un modulo
può comunque riaprire errori PHPStan reali su un consumer (qui: un test) che non è stato
aggiornato nello stesso commit. La ricognizione (`git status`, `docs/chat/INDEX.md`) prima di
ogni sessione PHPStan resta obbligatoria anche su moduli "già chiusi".

## Dev Agent Record

### Implementation

Convertiti 3 call site in `tests/Unit/LangCoverageBoostTest.php` da chiamata statica a
istanziazione + chiamata di istanza (`TranslationFileResource`, `TranslationFileForm`,
`TranslationFileInfolist`).

### Verification (2026-09-07)

- PHPStan `Modules/Lang` cold: PASS, 3 → 0 errori, config root invariata.
- `php -l` sul file: PASS.
- Pest mirato: 14 passed, 58 assertions.
- PHPMD scoped: solo debito preesistente, nessun finding sulle righe toccate.
- Pint scoped: passed.

### File List

- `Modules/Lang/tests/Unit/LangCoverageBoostTest.php`
- `Modules/Lang/docs/coverage.md`
- `Modules/Lang/docs/stories/7.4.phpstan-canonical-schemas-static-call-fix.story.md`
